[project]
name = "sft-to-rl-demo"
version = "0.1.0"
description = "SFT -> DPO -> GRPO and pure-RL demos for the AI Yatra 'Training Models to Think Better' session"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.13.0",
    "accelerate>=1.2.0",
    "transformers>=5.16.0",
    "peft>=0.20.0",
    "trl>=1.12.0",
    "datasets>=5.0.0",
    "python-dateutil>=2.9.0",
]
